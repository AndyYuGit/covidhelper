spring:
  datasource:
    url: jdbc:mysql://localhost:3306/covidhelper
    username: root
    password:
  redis:
    host: ${REDIS_HOST:localhost}
    port: ${REDIS_PORT:6379}
    password: ${REDIS_PASSWORD:}
    ssl: ${REDIS_SSL:false}
    database: ${REDIS_DATABASE:1}
    timeout: 10000ms
  jpa:
    open-in-view: false
